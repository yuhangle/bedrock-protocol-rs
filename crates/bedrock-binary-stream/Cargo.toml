[package]
name = "bedrock-binary-stream"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Binary stream serialization core for the Bedrock protocol"

[dependencies]
bedrock-common = { path = "../bedrock-common" }

[dev-dependencies]
hex = "0.4"
