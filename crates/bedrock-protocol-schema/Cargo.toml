[package]
name = "bedrock-protocol-schema"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Parse protocol-docs JSON schemas for Minecraft Bedrock Edition"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
